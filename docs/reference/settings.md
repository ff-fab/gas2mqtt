# Settings

Application settings, loaded from environment variables, `.env` files, and CLI flags.
Extends the cosalette `Settings` base with gas2mqtt-specific fields.

::: gas2mqtt.settings.Gas2MqttSettings
    options:
      show_bases: true
      members_order: source
